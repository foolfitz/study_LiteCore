# AGENTS.md

給在這個 repo 工作的 AI agent 的規則。
（`CLAUDE.md` 是這份的符號連結，兩邊讀到的是同一份。）

## 上游回報與證據一律用英文

以下**全部用英文寫**：

- `findings/evidence/**` —— 證據檔、log 標註、README、判讀說明
- `findings/drafts/*-bugzilla.txt` 與任何要送到上游的文字（commit message、patch
  說明、郵件）

理由：這兩類的讀者是上游——Bugzilla 的維護者、開附件來看的人。中文夾在裡面等於
要對方先做一次翻譯才能判斷，而我們送過去的目的就是讓對方能直接判斷。

**不受此限**，繼續用 zh-TW：`devlog/`、`research/`、`specs/`，以及草稿裡標明
「內部備註（不送出）」的段落。那些的讀者是我們自己，而且會回頭讀。

## 交接文件用英文（2026-08-19 起）

`handoff/**` 一律用英文寫。

理由：這一類的**唯一讀者是下一個接手的 agent**。使用者說了不會真的去看，所以
中文在這裡買不到任何可讀性，只是在 agent 與它引用的東西之間多墊一層翻譯——交接
文件裡九成的內容（檢查名、檔名、符號名、log 片段、finding 的判讀）本來就已經是
英文，正文跟著用英文，引用與敘述才會是同一種語言。

這條**只管檔案**：跟使用者對話、`devlog/`、`research/`、`specs/` 仍然 zh-TW。
既有的交接文件不回頭翻譯，只約束之後新寫的。

## Bugzilla 草稿不要為了寬度手動斷行

散文**一段就是一行**，讓 Bugzilla 自己軟斷行。不要再斷在 72 欄或 80 欄。

依據（2026-08-15 對 bugs.documentfoundation.org 實測）：

- 留言渲染在 `<pre class="bz_comment_text">`，`white-space: pre-wrap`——我們打的
  換行**會被原樣保留**。所以硬斷行不是「反正會被攤平」，它真的會顯示成硬斷行。
- 欄寬是 CSS `width: 50em`，等寬字前進寬實測 0.602em，等於固定 **83 個半形字元**。
  因為是 `em`，不隨視窗寬、縮放、字級改變；視窗縮到 390px 量仍是 83 欄，頁面改成
  整頁橫向捲動，沒有響應式重排。
- 上游沒有人這樣寫：抽 12 張單、31 則留言，硬斷行 **0 則**；最長散文行的中位數是
  215 字元，p90 是 534。
- 83 不是任何規矩，是 50em ÷ 0.6em 掉出來的餘數。真正有過的規矩是打孔卡的 80 和
  FORTRAN／郵件的 72。硬斷行是郵件年代的化石——那時 bug 留言從郵件閘道進來，本來
  就已經斷在 72 欄；現在是在瀏覽器 textarea 裡打字，HTML 預設 `wrap="soft"` 送出時
  不插入換行字元，要硬斷行反而得多花力氣手動做。

### 例外：換行屬於資料本身的，原樣保留

堆疊追蹤、程式碼片段、建置日誌、diff、makefile 片段——這些的換行是內容自己的結構，
不是我們為了遷就寬度加的。**不要重排它們。**

這類內容若單行超過 83 欄，處方是**把那一行改短**（例如砍掉路徑前綴、縮短欄位），
不是折成兩行。

### 不要把表格塞進留言

那個框沒有橫向捲軸也不重排，超過 83 欄會從空白處被折斷，對齊全毀。比較表、雜湊
清單這類東西改成**附件**，或改寫成散文。

---

既有草稿不回頭改（2026-08-15 的決定）。以上只約束之後新寫的。

量測方法可重跑：留言原文走 REST API（`/rest/bug/{id}/comment`，不受反機器人擋），
渲染規則要用真瀏覽器量 `getComputedStyle`——`curl` 抓 HTML 會被 Anubis 擋掉。

---

# 工作結論（2026-08-22）

## 現況：**v4 連結等使用者跑一行指令**

貨載做完、閘門全綠、樹是乾淨的。**連結那一步是使用者的**：

```bash
cd /home/jiajun/LibreOffice/study_LiteCore/wasm_sdk_probe
make ALLOW_FROZEN_RELINK=1 dist/profiles/e2-editor-v4/sdk-manifest.json
```

手冊：`handoff/RUNBOOK-relink-v4.md`（含連結後必做的四件事）。
**輸出要看 `withheld: ["select-all"]`**；那一格若是空的就停手——被扣住的動作帶著
gesture 進了 manifest，而 mask 收不回 manifest 已經給出去的東西。

出貨內容：後繼身分 `e2-editor-v4`、ABI 4、id 1–15 逐字繼承、16–21 往後 append；
redo 走 document 層沒有 wire id；select-all 與跨段刪除**出貨但扣住**；
a11y 誠實化、32,767 拒絕、finding 068 的修法（免費搭車）。

## 這一輪逼出來的規矩（會重複發生的那幾種）

**界限要跟著清單走，否則它靜默地全面失效。**`kMaxInternalEditorAction` 當時是 21，
而上一個 commit 剛加了 internal action 22。超過界限，`editorSetActionGestures` 直接
return（manifest 的 mask 從沒寫入），`editorGesturePermitted` 回 false——**manifest
說給了，引擎無條件拒絕**。是在建置之前讀界限才抓到的。

**「扣住」和「沒寫」是相反的意思，不是同義詞。**引擎在第一次 mask 呼叫時把每一格
初始化成**全許可**再往下交集。所以 manifest 裡**省略**一個動作＝把它全開出去；
扣住必須寫成 `gestures: []`（present but empty）。別「順手清掉」一個看起來沒用的
dark action。

**驗收條件是為某個處方寫的；換了處方就要改條件，並寫下理由。**這一輪有三個佇列項
的檢查釘的是我們沒採用的處方。改寫不是放寬——但**絕不能不寫理由就改**。
順帶：那個 redo 的檢查在 worker 多了一行「引用該字串」的註解時就 DRIFT 了，
**子字串斷言當證據就值這麼多**。

**間歇缺陷：次數就是量測，一次跑會給你一個有信心而且錯的結論。**finding 068 的臂
第一次 FAIL、第二次 PASS，同一支臂同一個頁面。修法前 2/7，修法後 5/5。
回歸網要寫成「N 次全過」，不要單次。

**像素分不出「沒畫」和「畫在別處」。**068 為此繞了五輪。畫布上「什麼都沒有」有四
種成因，長得完全一樣。**去問狀態，不要數像素**——而且要問到**引擎**那一側：我第三
次還是錯，因為我量了頁面的 snapshot 然後把它當成引擎的狀態。

**改動之前先問這個檔案綁著誰的判定。**068 的修法最自然的位置是
`EditorSession._handleEngineEvent`，而那個檔案綁著 **E1-C 的判定**
（`187706b2…`）——`check_e1_c_bundle_intact.py` 擋下來了。子類別覆寫拿得到同一個
事件，沒有那個代價。**是樹擋下來的，不是我想到的。**

**殼層 generation 要等該輪的殼層改完再凍。**同一天凍了 v28、v29、v30 三次，其中
v28 是在還有東西要改的時候凍的。號碼很便宜，但「凍了之後才發現還要改」會誘使你去
覆寫一個已經存在的 generation——那正是守衛存在要拒絕的事。

**archive 的名字要能識別它綁的東西。**`build/archive/e2-editor-v3-29ec627b/` 用出貨
的 wasm 雜湊命名，但四個檔裡有兩個不是出貨的那份；真正逐位元相同的是
`e2-editor-v3-29ec627b-worker-a9afbc01/`。**一顆 profile 是五個綁定身分，不是一個
artifact。**已加 `ATTRIBUTION.md`。

## 068／069：使用者確認過了，而過程本身是教訓

**068 已由使用者親眼確認修好**（2026-08-22，8792 預覽）。**069（輸入框躺在文件底部、
IME 組字把整頁拉下去）同日回報、同日修並量過**，回歸網 32 格 `ok: True`。

中間繞的那一圈值得記著：**我第一次交給使用者測的是半個修法。**068 的修法有兩半，
session 那半在 `dist/` 裡、worker 那半在**凍結的 v3 profile** 裡——使用者測到的是
「聽的人裝好了但沒有人廣播」，行為完全沒變。**凍結的 profile 會把修法擋在門外，而
這件事要講在最前面，不是埋在摘要裡。**處置：mirror 從原始碼換 worker 進去，並在頁面
上加一條**綠色橫幅**寫明「這是哪一版」——「使用者到底測了哪一份」不該再靠猜。

**harness 的路不是使用者的路，這一輪又踩到一次。**我的 `commit()` 走產品的插入欄位
＋工具列按鈕，使用者走真的鍵盤打進 sink。狀態臂 5/5 綠、使用者說沒變，兩邊都沒說謊
——量的不是同一條路。補了 `caret-after-real-typing`（CDP 逐字送鍵）之後才對得上。

**新檢查第一次紅，先確認它是紅在產品上還是紅在自己的門檻上。**069 的判準原本寫成
「捲動小於 50px」，實測 125px 判 FAIL——照它自己的規則是對的，但沒有用：**像素分不出
「捲到游標」和「捲過頭到文件末端」**。改成占可捲動範圍的比例（15% 對 99.5%）才分得
開。失敗的那一版留在證據裡。

---

# 驗收判準的形式（2026-08-29 立）

來源是隔壁 `study_OxODF/odf-rs` 從 ADR-0067 十二輪、ADR-0076 九輪不收斂萃取出的
方法紀錄。那份文件明說**不外推**到別的專案，所以這裡不抄條文，只留 2026-08-29
在**這棵樹上實際起作用**的幾條，每條附本樹自己的實例。

適用範圍：閘門判準、佇列項的期望、檢查的驗收條件。**不是**程式風格。

## 1. 判準有三種形式，只有兩種能當硬性條件

| 形式 | 例 | 可當硬性判準 |
| --- | --- | :---: |
| **逐實例性質**——對還不存在的成員自動成立 | 「每一份 soak 報告都要帶候選頁的 sha256」 | **可**（最強） |
| **工具封閉的枚舉**——清單由工具自真值源生成 | 「`report["checks"]` 裡每個 id 都要在 `MUTATIONS` 有一筆」——加第 41 個 check 它就紅 | **可** |
| **手工枚舉**——成員住在別處 | 「影響面就這幾處」「拒絕原因就這幾種」 | **不可**，只能當一次性量測並登錄 |

**判別法**（靜態）：成員能不能在**不編輯宣稱所在之處**的情況下改變？能，它就是鏡像。

**判別法**（動態，比較可靠）：**往真值源塞一個虛構的新成員，看哪個檢查會轉紅。**
逐實例性質：沒有檢查需要轉紅，而宣稱仍為真。手工枚舉：零檢查轉紅，而宣稱已假。

## 2. 判準不得量化在「未來的一次判斷」上

寫成「錄下它唸了什麼」「等到穩定為止」的條件，**是由當場在場的人決定過不過**。

- 已踩過：「a11y 血統的缺陷發現率還沒平緩」——只能靠再等來否證，每個乾淨的日子都
  讓它**感覺**更真而證明不了任何事。2026-08-27 換成十二筆固定條件。
- 又踩過：閘門條件 4 寫「用 Orca 跑一輪，錄下它唸了什麼」，**沒有寫什麼算過**。
  2026-08-29 拆成機器可驗的 4a（七條，先釘死）與人類殘餘的 4b。

**每個判準都要有「如果這件事永遠沒做」的預設結論**，寫在判準旁邊。

## 3.「有行為而沒有東西守著」和「沒有東西可守」是兩個登錄

`NOT_ESTABLISHED`、`knownRed`、「宣告但未量測」都是登錄，都要說清楚是哪一種。

實例（**我 2026-08-29 判錯過一次**）：兩個長期 NE 的檢查，我說「兩格在兩個 profile
上都沒量到東西」。查下去是兩個**不同**的案子——

- `notice-action-recovers-the-session` 是**設計好的哨兵**（`recoveryPairing.held`），
  能力本身在每次 plain run 都被別的檢查驅動；
- `a-refused-action-is-reported-and-changes-nothing` 是**建構上只在 diagnostic
  可判**（`refusal_induced = bool(args.refusal_diagnostic)`），出貨組態沒有拒絕可觀察。

把兩者寫成同一句話，會把「有洞」和「沒洞」一起說錯。

## 4. 撤回宣稱，不要造機械去維持它

判準的前提被證偽時，**刪掉那條判準**，不要補一層機制讓它繼續成立。

實例：切分探針的判準 3 要求「開需要 fallback 字型的文件會觸發抓取」。查前提才發現
**這個 SDK 從來沒有按需載入路徑**（finding 085），該性質在**任何** profile 上都不
成立，v8 也一樣。判準被**撤銷**改寫成 3′，不是打補丁。

**自察條件**：落地一份清單時若發現自己在寫**新機制**（不是測試、不是文字），停手回報。

## 5. 工單要問「找一個什麼都不會轉紅的變異」，而且要普查不要抽樣

問「這句宣稱是不是真的」，答案通常都是真的；缺的是「它不再為真時會不會有東西告訴你」。

實例：對 40 個產品路徑檢查 × 43 個 mutation 做普查——34 個有專屬 mutation、
1 個只有 collateral、**5 個一個都沒有**（其中 4 個集中在 inline format 一族）。
抽樣看不到這個分布。

**普查之前先問母數是第 1 節的哪一種形式。**

## 6. 綠燈不是證據，先讓它紅一次

自己寫的檢查器給綠燈，只證明它會回傳綠。**落地前先做出它的紅案。**

- `check_4a.py`：四個紅案覆蓋六條判準（給錯 sha、拿候選當對照組、只給兩次 run、
  拿對照組當候選）。
- 切分對帳探針：把 metadata 最後一筆砍一個位元組就紅。
- 普查腳本要**自己檢查非空洞性**：目錄改名時要紅，而不是搜了零個檔案回報「零」。

## 7. 驗宣稱本身，不要驗代理；能看就不要推

**推導**（從 metadata 算出掛載了哪些字型）與**讀取**（真的去讀 worker 的
`Module.FS`）在 2026-08-29 給出完全一致的答案——但堅持照字面讀是對的，因為過程中
我的讀取器有 bug，**錯誤訊息剛好把正確答案原樣印出來所以當場抓到**。若那個錯誤寫成
漂亮的一行「mount read failed」，我會退回推導並宣稱直接讀不可行。

**推論**：錯誤訊息帶酬載，不要帶酬載的摘要。

## 8. 終止宣告不得由起草方的判斷承載

「我跑了十二次都很乾淨」不是可查核的東西。判準若由人敘述來滿足，就補一支**讀既有
產物**的工具（形式二），讓別人重跑一行就能判。

2026-08-29 的作法：探針四項判準全過之後**沒有自行宣告**候選變更，因為後果是身分
變更＋作廢五筆已押的 run；把產物送去裁決，由裁決確認後才執行。

## 9. 跑動中的閘門可以加判準，但要四個條件同時成立

「判準要在能滿足它的執行之前釘死」禁止的是**事後移動**，不是**新增**。新增要：

1. **先於滿足**——寫在任何會算進它的執行之前；
2. **被逼出來且指名**——寫出是哪個量測或缺陷逼出它的，「我想到」不算；
3. **通過形式檢驗**——第 1 節的形式一或二；否則放進「工作項」不放進閘門；
4. **有截止**——第 N 筆乾淨執行一落地就封閉，之後的顧慮變成 finding 或事後工作。

**計數語意要一起寫**：哪些新增會讓計數歸零（改動「乾淨」的定義的）、哪些不會。

## 10. 閘門文件只准 append

更正寫在後面並指名它取代什麼，原文留著。這樣 `git diff` 只有新增行，凍結是可稽核的。

2026-08-29 一天之內用這個機制更正了四件事，其中三件是**本輪自己寫錯的**：
「on demand」（實際是永不載入）、「三倍大的核心映像」（實際 writer+calc 只值 5.5 MiB）、
「十二個 profile 共用」（實際 34 個）。

## 11. 起草方的實測失準率（供工單引用）

2026-08-29 一個 session 內，如實登錄：

| 項 | 紀錄 |
| --- | --- |
| 兩個長期 NE 的定性 | **1/2 錯**——其一是設計好的哨兵，我說成沒人守的洞 |
| 外包工單的前提 | 提供了一個假前提（「v8 延後載入」），照抄 plan 未查 |
| 一個判準的前提 | 該性質從未存在於任何 profile |
| 覆核者指出的檔案路徑 | 我先認為它錯，查下去是**我**看漏了同名的第二份 |
| 自己寫的讀取器 | CDP 信封多剝一層，靠錯誤訊息帶酬載才當場抓到 |
| 儀器設計 | 墨帶統計對本次比較零鑑別力（頁面邊框讓每列都有墨） |

**結論不是「起草方不可信」，是「終止宣告不該由起草方的判斷承載」**（第 8 節）。
