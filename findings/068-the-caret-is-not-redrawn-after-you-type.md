# 068 — 打完字之後**畫布上沒有游標**，要等到游標動一下它才出現

> **2026-08-22 改寫。**這張單原本的標題是「游標被畫在錯的位置（偏左一個字元）」，
> 那是 operator 的描述加上我的推論。量完之後**主張換了**：量到的不是「畫錯位置」，
> 是**打完字之後那一行上根本沒有游標**；而游標一旦動過，它出現的位置是**對的**。
> operator 描述的「在最後一個字元前面」**沒有被重現**，見〈還沒解釋的部分〉。

| | |
|---|---|
| **狀態** | **已確認，未修**（2026-08-22 量完）——主張已改寫 |
| **Bugzilla** | —（**不是上游**：模型正確、只有繪製錯，而繪製在產品頁裡） |
| **發現日** | 2026-08-21 operator 首報，2026-08-22 同一人在修過 [066](066-the-toolbar-takes-the-keyboard-and-never-gives-it-back.md) 之後**再次**看到 |
| **嚴重度** | **中**——編輯正確性沒有受影響（量到的），但**打完字之後使用者看不到游標在哪裡**，而打字正是最常做的事 |
| **可重現** | operator 兩天各一次，同樣的描述；**截圖有** |
| **是否上游** | **否** |

## 現象

**量到的**：在一個空段落裡打一段字，什麼都不要再按——**畫布上那一行沒有游標**。
按一下方向鍵（或任何會動到游標的動作），它就出現了，而且出現在對的位置。

**operator 原話**（2026-08-21，2026-08-22 修過 066 之後再次確認）：

> 「游標應該在位於文字的後面，但它是位於**最後一個字元的前面**」

截圖：`findings/evidence/068/operator-screenshot-2026-08-22.png`。
**他描述的是位置錯，我量到的是沒有畫**——兩者對不起來，見〈還沒解釋的部分〉。

## 已經量到的：**模型是對的，壞的是畫**

這兩件事是很不一樣的缺陷，而分開它們只要一次存檔。**如果游標的模型位置真的往左偏
一個字元，接下來打的字就會落在偏左的位置。**

臂（`caret-model-or-drawing`，`findings/evidence/068/model-position-is-correct.json`）：
空段落 → 打 `CARETONE` → 再打 `CARETTWO` → 存檔。

```xml
<text:p text:style-name="P3">CARETONECARETTWO1-LC-NUMBER-ONE</text:p>
```

**連續、順序正確**（`secondLandedInsideFirst: false`）。operator 自己那一輪的檔也
一致：`r2-caret.odt` 裡是乾淨的 `<text:p text:style-name="P1">CARETHERE</text:p>`。

所以：

- **編輯正確性沒有受影響。**打字落在該落的地方，存出來的文件是對的。
- 壞的是**畫布上那一根游標的 x**。

判準刻意用**文件**而不是畫布：這一條的主題是像素沒錯，但**像素分不出這兩種缺陷，
文件分得出來**。

## 量完了（2026-08-22）：不是位置錯，是**沒有畫**

一份 24 字元的標記獨自待在自己的段落裡（前置條件有查，不是假設），重繪**held
still**（`renders: 0, suppressed: 10`——就是這支探針第一個臂裝的那個抑制鉤，這次
反過來用：那時候問重繪會不會毀掉格式，這次用它把背景釘住，好讓一根一像素的痕跡
看得見）。

然後**直接問**每一次讀取：哪些欄位有夠高的連續暗列？（這個字體在這個縮放下字形
約 11 列，游標 15 列，所以 13 是分得開的門檻。）

| | 那一行上高度足夠的痕跡 |
|---|---|
| **打完字之後** | **一個都沒有** |
| 游標左移五格之後 | **正好一個**：欄 257，第 251–265 列，15 列高 |

集合相減：`onlyAfterTyping: []`、`onlyAfterMove: [[257, 251, 15]]`。

**而且它出現的位置是對的**：那一行的字形 ink 是 82…302，24 個字元，一個字元
9.57px，所以左移五格之後應該在 302 − 5×9.57 ≈ **254.2**，實測 **257**——差不到
半個字元。`caretAfterMoveIsWhereExpected: true`。

**正向對照就是後面那一格**：同一個掃描找得到游標，所以它在前一格的沉默是「那裡
沒有東西」，不是「這個掃描看不見游標」。

**插入欄位與鍵盤兩條路都試了，結果一樣。**（`same-result-via-the-insert-field.json`
是插入欄位那一輪；最終那一輪是鍵盤。）這一次路徑不是變因——066 與 067 都是路徑
造成的，所以先排除掉。

## 為什麼前五次量不到

每一次的失敗原因都留著，因為它們是同一件事的不同面向：

| 方法 | 為什麼失敗 |
|---|---|
| `LINE_INK` 兩次相減 | 餵給 `caret_from_columns` 的讀取器少一個 `band` 欄位，於是滿是文字的帶被判成「沒有文字」 |
| `CARET_COLUMNS` 相減，左移 1 | 訊號太小 |
| 左移 5 拉開距離 | 被**頁框**淹掉——第 13、14、710、711 欄各是整條帶高的直線 |
| 等連續兩次讀數相同 | 頁框仍然不同：不是重繪中途的抖動 |
| 用顏色單次分離（`#1a1a1a`） | 只找到四個零星像素。**原因是算術**：游標寬度是 `Math.round(scaleX * 15)` ＝ **1 像素**，畫在小數 x 上，`fillRect` 會把它反鋸齒到兩欄，**沒有任何像素是正好 (26,26,26)** |
| 量 operator 的截圖 | 360 寬對畫布 725，縮了一半，一像素的游標和字母直筆分不開 |

**成功的做法是兩件事一起**：把重繪釘住（消掉「新的 paintTile 會整張重新反鋸齒」
——量到過 725 欄裡有 686 欄在變），然後**不要相減**，直接問「哪一欄有夠高的連續
暗列」。

## 還沒解釋的部分

**operator 說游標在「最後一個字元的前面」，我量到的是「那一行上沒有游標」。**
這兩件事對不起來，而我不打算硬把它們湊在一起。

- 一個可能：他截圖時看到的是最後一個字母的直筆，而不是游標。他的截圖縮了一半，
  那一行有 11 個滿高欄位、全都落在字母筆畫上——**那張圖分辨不出來**，這一輪已經
  證明了。
- 另一個可能：他的操作序列和探針不同（例如中間有別的狀態更新讓 `paint()` 跑過），
  於是他確實看到一根畫在舊位置的游標。

**要分辨需要一張原始解析度的截圖**，那還是這張單原本就寫著的下一步——只是現在
問的問題換了：不是「游標偏多少」，而是「你看到的那一根，到底是游標還是字母」。

## 歸因

模型正確、繪製那一側有問題，而繪製在產品頁裡：`paint()` 拿 `editorState.caret` 的矩形，
`box()` 換算成畫布座標，`fillRect(x, y, …)`（`web/e2-editor-app.js`）。
而 `paint()` **只在狀態更新時被呼叫**。所以最可能的方向是：一次文字提交之後，
**帶著新游標矩形的狀態更新沒有到**，於是沒有人重畫游標。**這是推論，不是量測**
——下一步就是去問那個狀態更新（見〈下一步〉）。

（順帶：這一條在 finding 058 之前根本看不到，因為那時候頁面**完全不畫游標**。）

## 這張單和 066 的關係

operator 在 2026-08-21 首報時，同一輪還報了「按『換行』之後游標沒有移到下一行」。
**2026-08-22 那一項變成正常了**（`r2-linebreak.odt`：
`<text:p>CARETHERE<text:line-break/>AFTERBREAK</text:p>`，而且 operator 說畫面上
也對）。中間唯一的變化是 066 的修法。**沒有獨立量過**，最可能的解釋是當時焦點
停在按鈕上、後續動作沒有落在他以為的地方——但那是推測。

游標位置這一條**不受 066 影響，兩天都在**。


## 下一步

1. **一張原始解析度的截圖**（CDP `Page.captureScreenshot` clip 到 canvas，或
   `canvas.toDataURL()`），用來判斷 operator 看到的那一根是游標還是字母筆畫。
2. **一格 `KNOWN_RED` 檢查**：打字之後那一行上必須有一個游標高度的痕跡。判準已經
   有了（就是這支探針的臂），還沒放進回歸網——這一輪已經在同一個檔案踩過三次順序
   相依，不在長時段的尾巴再插一格。
3. 處方的方向：`paint()` 是從 `editorState.caret` 畫的，而它只在狀態更新時被呼叫。
   **一次文字提交之後，帶著新游標矩形的狀態更新有沒有到？**那是下一個要問的，
   而且它是產品那一側。
