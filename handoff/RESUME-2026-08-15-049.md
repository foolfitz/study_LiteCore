# 中斷點 — 2026-08-15，任務 #49 走到「預測已 commit，探針還沒寫」

工作樹乾淨，背景服務都停了。接續請先讀
[`HANDOFF-2026-08-15-session-state.md`](HANDOFF-2026-08-15-session-state.md)，本份只記這次中斷的位置。

## 這一段做完的

| commit | |
|---|---|
| `21e95a5` | **#50 完成**：`demo-structure` 有復原鈕，走 Document SDK 的 `document_.undo()`（不是診斷 ABI 的 undo，那個仍在 `FORBIDDEN`）。判準是存出來的 ODT，證據在 `findings/evidence/sdk-e2/discovery/demo-structure-undo/`，`python3 check.py` 可重跑。引擎未重連結（`make -n` 只有兩行 `cp`，前後都是 `c89f069e`）。順帶更正 `demo-structure.html` 上「E2-A 還沒有總判定」這句過時的話 |
| `a272f74` | **#49 的預先登記**：`findings/evidence/sdk-e2/discovery/049-selection-after-format/PREDICTION.md`，五臂、五種結局，**寫在任何一次執行之前** |

## #49 目前的推論（**還沒有量到**）

引擎的範圍選取送 **`RESET` ＋ `END`，沒有 `START`**（`probe_engine.cpp:3805`–`3812`）。
核心那邊 `.uno:SelectText` → `FN_SELECT_PARA` → `EndPara(true)` → `MoveCursor(true)`
→ **`SttSelect()` 設 `m_bInSelect = true`，而這條路徑沒有人呼叫 `EndSelect()`**；
兩次 `RESET` 都因 `bCreateSelection == false` 而不碰這個旗標；
最後呼叫端的 `END` 呼叫 `SttSelect()`，被 `if (m_bInSelect) return;` 擋掉，
**`SetMark()` 從未執行 → 沒有 mark → 沒有選取**。
逐行出處寫在 PREDICTION.md 的表。

## 下一步就是這一件

**寫 `wasm_sdk_probe/tools/f049_native_select_after_format.cpp` 並跑起來。**

- 原生 build **在**：`../build-native-26-8/instdir/program`。
- 抄 `tools/f039_native_caret_reset.cpp` 的骨架（回呼追蹤、`locateText` 用
  `.uno:ExecuteSearch` 找 `E1-LC-ISOLATED`、`emitArm` 印 JSON），
  執行腳本抄 `tools/run_f039_native.sh`。
- 每一臂**重新載入文件**，免得上一臂的格式動作污染下一臂。
- fixture：`test-docs/e1/list-contexts.odt`；
  `.uno:DefaultBullet` 的參數是 `{"On":{"type":"boolean","value":true}}`。

### 動手前必須先做的一件事：**修正臂 E 的預測，另外 commit**

PREDICTION.md committed 的版本說臂 E（full barrier ＋ `.uno:Escape`）**會選得到**。
**那個前提在執行前就被讀壞了**：`FN_ESCAPE` 只有在
`m_pWrtShell->HasSelection()` 為真時才走到 `EnterStdMode()`
（`sw/source/uibase/uiview/view2.cxx:1232` 的 `else if`、`:1251` 的呼叫），
而 barrier 還原完剛好**沒有**選取，會掉到最後那個切換全螢幕的分支。

所以：**臂 E 改預測為「也選不到」**，並新增

> **臂 F** `full-barrier + go-left`：同 D，最後多派送一次 `.uno:GoLeft`
> （`sw/sdi/swriter.sdi:1467` → `FN_CHAR_LEFT` → `SwWrtShell::Left(..., bSelect=false, ...)`
> → `ShellMoveCursor(this, false)` → `MoveCursor(false)` → **`EndSelect()`**）。
> **預測：選得到。**

修正要**在跑之前**單獨 commit，不要就地改掉再假裝原本就這樣寫。

## 還開著的（順序不變）

| # | | |
|---|---|---|
| **49** | 進行中，見上 | demo 目標第一優先 |
| **48** | 寫 SPEC-E2-B | 被 #49 擋 |
| **46** | finding 040 上游未送 | **送出要使用者按**；重複單查詢日期還是 08-14，送前要重查；Component 欄位待確認；還缺一支非主執行緒 `doc_destroy` 的最小重現 |
| — | 039 產品對照那五個數字仍**未落地** | 這次沒動到 |

## 環境備註

- 這一段用 Playwright MCP 開過 chromium（跑 #50 驗收），**已關**；
  使用者自己的 Chrome 沒碰。
- `web/serve.py --port 8781` 曾在背景跑，**已停**。
- 三個凍結 artifact 全程未動：`835b453d`／`679def61`／`c89f069e`。
