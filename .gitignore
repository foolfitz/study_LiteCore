# study_LiteCore 是一個「容器」目錄，不是一個乾淨的專案根：
#   * 總共 69 GB
#   * 裡面直接包著五個獨立的 git repo／worktree
#     （libreoffice-25-2、libreoffice-26-2、libreoffice-26-8、collabora-25.04、cool-26-04）
#   * 另有 43 GB 的建置樹（wasm-lite/、native-lok-26-8/、build-native-26-8/、
#     wasm_sdk_probe/dist、wasm_sdk_probe/build）
#
# 所以這份檔案刻意寫成 **allowlist**：預設忽略一切，再逐項放行我們自己寫的東西。
# 反過來寫（列黑名單）的話，將來任何新長出來的建置目錄都會預設被收進版控，
# 而且要等到有人想起來才會補上——這裡的目錄大到不能靠記性。
#
# 不收版控但**不可遺失**的東西：`wasm_sdk_probe/dist/profiles/`（1.7 GB 的凍結
# artifact，證據綁定的錨點，且至少 R5 profile 無法從 source 重建）。
# 那些走另外的 checksum 備份，見 `docs/ARTIFACT-BACKUP.md`。

/*
!/.gitignore
!/*.md
!/LICENSE

!/specs
!/findings
!/docs
!/Skills
!/wasm_sdk_probe
!/wasm-lite
!/devlog
!/research
!/handoff

# --- wasm_sdk_probe：只收原始碼 ---
# dist/（6.1 GB 建置產物）與 build/（1.3 GB 中繼物）不進版控；
# 根目錄那些 build-*.log／link-*.time 也是建置副產物。
/wasm_sdk_probe/*
!/wasm_sdk_probe/.gitignore
!/wasm_sdk_probe/Makefile
!/wasm_sdk_probe/package.json
!/wasm_sdk_probe/*.md
!/wasm_sdk_probe/assets
!/wasm_sdk_probe/collaboration
!/wasm_sdk_probe/delivery
!/wasm_sdk_probe/e1
!/wasm_sdk_probe/e2
!/wasm_sdk_probe/editor-shell
!/wasm_sdk_probe/editor-shell-v2
!/wasm_sdk_probe/input
!/wasm_sdk_probe/provider-sdk
!/wasm_sdk_probe/providers
!/wasm_sdk_probe/r7
!/wasm_sdk_probe/r8
!/wasm_sdk_probe/reader-shell
!/wasm_sdk_probe/sdk
!/wasm_sdk_probe/src
!/wasm_sdk_probe/test-docs
!/wasm_sdk_probe/tests
!/wasm_sdk_probe/tools
!/wasm_sdk_probe/web

# --- wasm-lite：只收 patch 快照與建置說明 ---
# tools/ 是 4.2 GB 的工具鏈，build*/ 與 logs/ 是建置樹。
/wasm-lite/*
!/wasm-lite/*.md
!/wasm-lite/build-wasm-lite.sh
!/wasm-lite/patches

# --- 一般雜訊 ---
# 注意：這裡**故意不寫** `*.log`。findings/evidence/ 底下的 `serve.log`、
# `geckodriver-*.log`、`*.log.txt` 都是證據本體（finding 023 的位元組對帳就靠
# serve.log），不是雜訊。
__pycache__/
*.pyc
*.o
